type Routes = { [key: string]: string };

const routes: Routes = {
  WELCOME: '/welcome',
  SIGNUP: '/signup',
  SIGNIN: '/signin',
  PROFILE: '/profile',
  DIARY: '/diary',
  PRODUCTS: '/products',
  EXERCISES: '/exercises',
};

export default routes;
